// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // TODO: Update if the production domain differs.
  site: 'https://www.revisionhomeimprovements.com',
});
